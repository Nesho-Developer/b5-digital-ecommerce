import { MainContentComponent } from './main-content.component';

describe('MainContentComponent', () => {
  let component: MainContentComponent;

  beforeEach(async () => {
    component = new MainContentComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
